<h1>Edit Profile</h1>

  <%= form_for @user do |form| %>
    <%= render partial: '/users/form', object: form %>
    <div class="submit-field">
      <%= form.submit "Submit", class: "btn btn-link btn-lg" %>
    </div>
  <% end %>


   <%= link_to 'Back', user_path %>
